# Example Learning Placeholder

This directory is a placeholder for example `teach` skill session records.
Replace it with real dated learning files when applying the manifest to a target
repository, or keep it only as a format reference in this template.

Use learning files named like:

- `YYYY-MM-DD-topic.md`
